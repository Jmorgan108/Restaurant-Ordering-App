<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jmorgan17037719.ac.uk.loginpagetest1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_fridge"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_fridge_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TablesActivity"></activity>
        <activity android:name=".MenuActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".ContactActivity" />
        <activity android:name=".AllergyActivity" />
        <activity android:name=".MoreActivity" />
        <activity android:name=".HomeScreenActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginStartActivity" />
    </application>

</manifest>